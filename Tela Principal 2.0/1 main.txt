import 'package:flutter/material.dart';
import 'package:loja_virtual/Screens/login_screen.dart';
import 'package:loja_virtual/Screens/principal.dart';
import 'package:loja_virtual/Screens/registration_screen.dart';
import 'package:loja_virtual/tabs/teste.dart';

import 'Screens/home_screen.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter´s Clothing',
      theme: ThemeData(

        primarySwatch: Colors.blue,
        primaryColor: Color(0xff2A236E),     //definindo cor primaria, é defido para vários objetos dentro do app

      ),
      debugShowCheckedModeBanner: false,    //removendo informação de debug da aplicação
      home: HomeScreen(),                   //chamando a homescreen

    );
  }
}
